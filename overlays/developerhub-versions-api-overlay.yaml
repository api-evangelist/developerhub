overlay: 1.0.0
info:
  title: API Evangelist enhancements — DeveloperHub.io Documentation Versions API
  version: 1.0.0
extends: developerhub-versions-api-openapi.yml
x-generated: '2026-09-06'
x-method: generated
x-source: derived from the operation descriptions in the extended document; no content invented
actions:
- target: $.info
  description: Attach the provider API reference as externalDocs and record the harvest source.
  update:
    x-apis-io-source: https://docs.developerhub.io/api.md
    x-apis-io-harvested: '2026-09-06'
- target: $
  description: Add externalDocs pointing at the provider API reference.
  update:
    externalDocs:
      description: DeveloperHub API reference
      url: https://docs.developerhub.io/api/ref
- target: $.paths['/version'].get
  description: Lift the rate limit stated in the operation description into a machine-readable extension.
  update:
    x-rate-limit:
      requests: 60
      window: 60 minutes
      scope: api-key
      source: operation description
- target: $.paths['/version/{id}'].put
  description: Lift the rate limit stated in the operation description into a machine-readable extension.
  update:
    x-rate-limit:
      requests: 3600
      window: 1 hour
      scope: api-key
      source: operation description
- target: $.paths['/version/{id}/clone'].post
  description: Lift the rate limit stated in the operation description into a machine-readable extension.
  update:
    x-rate-limit:
      requests: 60
      window: 60 minutes
      scope: api-key
      source: operation description
- target: $.paths['/version/{id}/report'].get
  description: Lift the rate limit stated in the operation description into a machine-readable extension.
  update:
    x-rate-limit:
      requests: 60
      window: 60 minutes
      scope: api-key
      source: operation description
- target: $.paths['/version/{id}/broken-links'].get
  description: Lift the rate limit stated in the operation description into a machine-readable extension.
  update:
    x-rate-limit:
      requests: 60
      window: 60 minutes
      scope: api-key
      source: operation description
